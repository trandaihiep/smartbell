#include <iostream>
#include <stdexcept>
#include <stdio.h>
#include <string>

std::string exec(const char* cmd);
bool checkrecord();
int key_search(const std::string& s, const std::string& key);
void record(std::string url, std::string dir);
